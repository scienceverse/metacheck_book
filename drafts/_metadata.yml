# Drafts are work-in-progress reference material copied from the package
# _stuff/ folder. They are NOT executed when the book is rendered: many depend
# on local data files or development state that is not bundled with this book.
# Code is shown as static source so the chapters remain readable without
# breaking the build.
execute:
  eval: false
